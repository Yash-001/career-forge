# database/

Flyway SQL migrations will be placed here as features are implemented.

Migrations live in `backend/src/main/resources/db/migration/` and follow the naming convention:

```
V1__create_users.sql
V2__create_profiles.sql
```

Seed data scripts for the demo account will also be stored here for reference.
